import 'package:flutter/material.dart';

class NewsItemCard extends StatelessWidget {
  const NewsItemCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
